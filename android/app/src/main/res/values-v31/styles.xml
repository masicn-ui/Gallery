<resources>
    <!-- Android 12+ (API 31+) splash screen configuration.
         Mirrors Playground's setup: build BootTheme directly on
         Theme.AppCompat.DayNight.NoActionBar (not the library's own Theme.BootSplash chain) and
         set every android:windowSplashScreen* attr explicitly. -->
    <style name="BootTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/bootsplash</item>

        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <item name="android:windowSplashScreenBackground">@color/bootsplash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/bootsplash_logo</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/bootsplash_background</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>

        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>
</resources>
